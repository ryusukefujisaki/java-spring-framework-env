networks:
  default:
    name: java_spring_framework_network
services:
  spring:
    build:
      context: spring
      args:
        APP_ROOT: $APP_ROOT
    ports:
      - 8080:8080
    container_name: spring
    volumes:
      - ./spring:$APP_ROOT
